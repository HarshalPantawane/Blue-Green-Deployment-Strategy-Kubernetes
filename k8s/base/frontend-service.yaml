apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: frontend
    version: blue # Active version
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
