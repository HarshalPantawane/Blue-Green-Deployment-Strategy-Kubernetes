apiVersion: v1
kind: Secret
metadata:
  name: food-delivery-secrets
  namespace: production
type: Opaque
stringData:
  # In a real environment, these would be injected securely (e.g., via AWS Secrets Manager or HashiCorp Vault)
  DB_HOST: "database-host"
  DB_USER: "admin"
  DB_PASSWORD: "supersecretpassword"
